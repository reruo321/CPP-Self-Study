#include <iostream>
#include <vector>

using namespace std;

void minStatDiff() {
	vector<vector<int>> stat_vec;
	int n;
	cin >> n;

	stat_vec.assign(n, vector<int>(n / 2, -1));
}

int main() {

	minStatDiff();

	return 0;
}